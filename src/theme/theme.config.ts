/* Theme configuration

This file defines the theme configuration for the application.

It imports the theme tokens and applies them to the application.
*/

import tokens from './tokens';

export default {
  tokens,
  // Add any additional theme configuration here
};